<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
</head>

<body>
    <h1><%= user.name %></h1>
    <h2><%= user.tickets %></h2>
    <form action="/tickets/<%=user._id%>" method="POST">
        <button>Get ticket</button>
    </form>
    <ul>
        <% for (let event of events){%>
            <li><%= event.title %> <%= event.prize %> <%= event.date %></li>
            <form action="/participate/<%= event._id %>/<%=user._id%>" method="POST">
                <button>use ticket</button>
            </form>
        <% }%>
    </ul>
    <footer>
    </footer>
</body>

</html>